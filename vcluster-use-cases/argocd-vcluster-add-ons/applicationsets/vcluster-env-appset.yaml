apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vcluster-env-config
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchLabels:
            env: dev
    - clusters:
        selector:
          matchLabels:
            env: qa
    - clusters:
        selector:
          matchLabels:
            env: prod
  template:
    metadata:
      name: wwt-app-{{.name}}
    spec:
      destination:
        namespace: wwt-app-{{.metadata.labels.env}}
        server: '{{.server}}'
      info:
        - name: GitHub Repo
          value: https://github.com/loft-demos/wwt-app/
      project: 'loft-{{ index .metadata.labels "demos.loft.sh/project" }}'
      source:
        path: vcluster-use-cases/argocd-vcluster-add-ons/manifests/{{.metadata.labels.env}}/
        repoURL: https://github.com/loft-demos/wwt-app.git
        targetRevision: main
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
